# [START gae_quickstart_yaml]
runtime: nodejs12

env_variables:
  DB_HOST: $DB_HOST
  DB_NAME: $DB_NAME
  DB_PASS: $DB_PASS
  DB_PORT: $DB_PORT
  DB_USER: $DB_USER

- url: /.*
  secure: always
  script: auto
# [END gae_quickstart_yaml]