# [START gae_quickstart_yaml]
runtime: nodejs10

handlers:
    - url: /
      http_headers:
        Access-Control-Allow-Origin: '*'

env_variables:
    DB_HOST: $DB_HOST
    DB_NAME: $DB_NAME
    DB_PASS: $DB_PASS
    DB_PORT: $DB_PORT
    DB_USER: $DB_USER
# [END gae_quickstart_yaml]